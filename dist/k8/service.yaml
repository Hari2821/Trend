apiVersion: v1
kind: Service
metadata:
  name: trend-service
  namespace: trend           # <-- add
  labels:
    app: trend
spec:
  type: LoadBalancer
  selector:
    app: trend
  ports:
    - port: 80
      targetPort: 3000

